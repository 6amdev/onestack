# upstream parse_backend {
#     server parse-server:1337;
# }

# upstream dashboard_backend {
#     server dashboard:4040;
# }

# server {
#     listen 80;
#     server_name _;
    
#     # Security headers
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
    
#     # Parse API
#     location /parse {
#         proxy_pass http://parse_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_cache_bypass $http_upgrade;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
    
#     # Dashboard - ไม่ต้อง rewrite
#     location /dashboard {
#         proxy_pass http://dashboard_backend:4040/;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
#         # WebSocket support
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_cache_bypass $http_upgrade;
#     }
    
#     # Dashboard assets
#     location /public {
#         proxy_pass http://dashboard_backend:4040/public;
#     }
    
#     location /login {
#         proxy_pass http://dashboard_backend:4040/login;
#     }
    
#     # Health check
#     location /health {
#         access_log off;
#         return 200 'healthy\n';
#         add_header Content-Type text/plain;
#     }
    
#     # Root - แก้ไขตรงนี้
#     location = / {
#         return 301 /dashboard;
#     }
    
#     location / {
#         try_files $uri $uri/ =404;
#     }
# }




# server {
#     listen 80;
#     server_name _;

#     # Parse API
#     location /parse {
#         proxy_pass http://parse-server:1337;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }

#     # Dashboard
#     location /dashboard {
#         proxy_pass http://dashboard:4040;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#     }

#     # Health check
#     location /health {
#         return 200 'healthy\n';
#         add_header Content-Type text/plain;
#     }

#     # Root
#     location / {
#         return 200 'OneStack is running!\n';
#         add_header Content-Type text/plain;
#     }
# }


server {
    listen 80;
    server_name _;
    
    client_max_body_size 20M;
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Parse API
    location /parse {
        proxy_pass http://parse-server:1337;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Dashboard
    location /dashboard/ {
        proxy_pass http://dashboard:4040/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    location /public {
        proxy_pass http://dashboard:4040/public;
    }
    
    location /login {
        proxy_pass http://dashboard:4040/login;
    }
    
    # Health check
    location /health {
        access_log off;
        return 200 'healthy\n';
        add_header Content-Type text/plain;
    }
    
    # Root
    location / {
        return 301 /dashboard/;
    }
}